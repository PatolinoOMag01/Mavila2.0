import { useEffect, useState } from "react";import { Link } from "react-router-dom";import PageTransition from "../components/PageTransition";import SEO from "../components/SEO";import "../styles/OrderConfirmed.css";
export default function OrderConfirmed(){const[order,setOrder]=useState(null);useEffect(()=>{setOrder(JSON.parse(localStorage.getItem("mavila-last-order")||"null"));},[]);return <PageTransition><SEO title="Pedido confirmado — MAVILA" description="Seu pedido demonstrativo MAVILA foi confirmado."/><main className="order-confirmed"><div className="order-confirmed-card"><div className="success-icon">✓</div><span>PEDIDO CONFIRMADO</span><h1>Fechou.</h1><p>Seu pedido demonstrativo foi salvo no histórico do MV Club.</p>{order&&<div className="confirmed-details"><div><span>Número</span><strong>{order.id}</strong></div><div><span>Total</span><strong>R$ {order.total.toLocaleString("pt-BR",{minimumFractionDigits:2})}</strong></div><div><span>Pagamento</span><strong>{order.payment.toUpperCase()}</strong></div></div>}<div className="confirmed-actions"><Link to="/profile">Ver meus pedidos</Link><Link to="/mv01">Continuar explorando</Link></div></div></main></PageTransition>}
